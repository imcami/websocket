import { Router } from "express";
import { ProductManager } from "../ProductManager";

const views = new views(src/products.txt)

const routerViews = Router()
const prodManager = new ProductManager('./products.txt')
